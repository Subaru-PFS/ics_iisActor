#!/bin/bash
port=24
if [ "$1" = "" ]
then
    echo "USAGE: fans [on,off]"
    exit
fi
if [ "$1" = "on" ] || [ "$1" = "ON" ]
then
    val=1
else
    val=0
fi
echo $val > /sys/class/gpio/gpio$port/value


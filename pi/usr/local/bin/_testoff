#!/bin/bash
# turns test mode off, and turns all devices off (does _alloff)
sysdir=/sys/class/gpio

# 8 output ports
# ch      1  2  3  4  5  6  7  8
for i in 13 17 22 23 24 25 26 27
do
    echo 0     > $sysdir/gpio$i/value
done
rm -f $readyfile $runningfile $coolingfile $testingfile 
